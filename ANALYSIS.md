# 线圈磁场计算代码 — 第40点附近磁场突变问题分析与修复

## 问题描述

在原始代码中，沿线圈曲线累计长度绘制的 B_z 磁场强度图在第40个采样点附近出现了不合理的突变（跳变）。第40点大约位于第一段圆弧（arc 1, r=0.6m, 70°）和第二段圆弧（arc 2, r=1.0m, 60°）的交界处。

## 根本原因分析

### Bug 1（主要原因）：`generateLocalFrameOrigins` 中直线段局部坐标系原点错误

**原代码（第152行）：**
```matlab
y_positions = linspace(-L_STRAIGHT/2, -L_STRAIGHT/2, n_segments + 1);
```

**问题：** `linspace` 的两个端点参数完全相同（都是 `-L_STRAIGHT/2 = -1.5`），导致所有直线段的局部坐标系原点都集中在 `y = -1.5` 处。

**影响：**
- 直线段（长度3m，从 y=-1.5 到 y=1.5）被4个梯形棱柱近似，但这4个棱柱的中心全部重叠在 y=-1.5
- 这导致直线段区域的场源贡献严重偏向一端，而非均匀分布
- 当采样点从直线段上半部分进入圆弧段时（约在第27-40点之间），距离错误放置的场源越来越远，而圆弧段场源放置正确
- 这种从"错误场源"到"正确场源"的过渡在第40点附近（圆弧1/2交界处）产生了明显的磁场突变

**修复：**
```matlab
y_positions = linspace(-L_STRAIGHT/2, L_STRAIGHT/2, n_segments + 1);
```

### Bug 2：`generateTrapezoidParameters` 中直线段棱柱半长度计算错误

**原代码：**
```matlab
b_vals(1:n_segments) = L_straight/4;
```

**问题：** 对于4段均分的直线段，每段全长为 `L_straight/4 = 0.75m`，半长度应为 `L_straight/(2*n_segments) = 0.375m`。原代码使用了全长作为半长度参数，使得每个棱柱的实际长度为 1.5m（本应为 0.75m），导致相邻棱柱之间产生严重重叠。

**影响：**
- 4个棱柱各覆盖1.5m，总覆盖范围远超直线段实际长度3m
- 重叠区域的场被重复计算，导致该区域磁场偏大
- 加剧了Bug 1造成的不均匀性

**修复：**
```matlab
b_vals(1:n_segments) = L_straight / (2 * n_segments);
```

### Bug 3（次要）：`generateCoilBasePoints` 中采样点在段交界处重复

**原代码：** 每段线圈（直线段和圆弧段）的 `linspace` 都包含首尾两个端点，而相邻段共享交界点，导致在直线段→圆弧1（约第27点）和圆弧1→圆弧2（约第40点）交界处各有一个重复采样点。

**影响：**
- 100个采样点中有2-3个是重复的，浪费采样资源
- 在累计长度均匀分布的绘图坐标中，重复点会占据不同的x坐标，可能造成视觉上的微小跳跃

**修复：** 在直线段和前两段圆弧的 `linspace` 中排除终点，仅保留最后一段圆弧的终点。

## 修复后的文件

- `coil_magnetic_field.m` — 包含所有修复的完整代码

## 为什么是第40点附近？

通过计算可以确认各段的采样点分布：

| 区段 | 长度 (m) | 采样点数 | 索引范围 |
|------|---------|---------|---------|
| 直线段 (y=0→1.5) | 1.50 | ~27 | 1–27 |
| 圆弧1 (0°→70°, r=0.6) | 0.73 | ~13 | 28–40 |
| 圆弧2 (70°→130°, r=1.0) | 1.05 | ~19 | 41–59 |
| 圆弧3 (130°→180°, r≈2.47) | 2.15 | ~41 | 60–100 |

第40点正好位于圆弧1的末端/圆弧2的起始位置。由于Bug 1导致的直线段场源位置错误，此处的场贡献突然从"以错误分布的直线段贡献为主"转变为"以正确分布的圆弧贡献为主"，从而产生突变。
